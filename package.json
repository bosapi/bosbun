{
  "name": "bosia-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*",
    "docs"
  ],
  "scripts": {
    "publish": "npm publish --workspace packages/bosia --access public",
    "publish:dry": "npm publish --workspace packages/bosia --dry-run"
  }
}
